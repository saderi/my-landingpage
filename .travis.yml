before_install:
  - sudo apt-get install -y lftp
language: node_js
node_js:
  - '8'
install:
  - npm install
script:
  - npm run build
deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master